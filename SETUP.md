# CineSnack — คู่มือตั้งค่า (pilot 1 สาขา)

4 ไฟล์ที่ต้องใช้:
- `index.html` — เว็บลูกค้า (เอาไว้ทำ QR ติดที่โต๊ะ/ป้าย)
- `counter.html` — เว็บเคาน์เตอร์ (เปิดค้างบน iPad พนักงาน)
- `display.html` — จอแสดงคิวให้ลูกค้าดู (แขวนจอที่เคาน์เตอร์) → `.../display.html`
- `report.html` — สรุปยอดขายหลังบ้าน (ไว้กระทบยอดกับ POS สำหรับผู้บริหาร) → `.../report.html`

## จอแสดงคิว (display.html) + โฆษณา
- เปิด `display.html` บนจอ/ทีวีที่เคาน์เตอร์ (แนวตั้งสวยสุด) → **แตะหน้าจอ 1 ครั้ง** เพื่อเข้าเต็มจอ + เปิดเสียงเรียกคิว
- คอลัมน์ซ้าย = "กำลังทำ" (ออเดอร์ที่เคาน์เตอร์ยืนยันแล้ว) · ขวา = "พร้อมรับ" (กด "จัดเสร็จ" แล้ว) มีเสียงเด้งตอนมีคิวใหม่พร้อมรับ
- **ใส่โฆษณาหมุนวน:** เอารูปโปรฯ ไปวางในโฟลเดอร์ `img/ads/` แล้วแก้ตัวแปร `ADS` ใน `display.html` เช่น
  `const ADS = ["img/ads/promo1.jpg", "img/ads/promo2.jpg"];`
  (เว้นว่าง `[]` = ใช้สไลด์ตัวอย่าง) · ปรับความเร็วสไลด์ที่ `AD_INTERVAL`

ทั้งคู่คุยกันผ่าน Firebase — ต้องตั้งค่า 1 ครั้ง แล้วใช้ได้เลย

---

## 1) สร้างโปรเจกต์ Firebase (ฟรี ~5 นาที)

1. ไป https://console.firebase.google.com → **Add project** ตั้งชื่อ เช่น `cinesnack` → ข้าม Google Analytics ได้
2. ในโปรเจกต์ กดไอคอน **`</>` (Web)** เพื่อ Add app → ตั้งชื่อ → **Register app**
3. มันจะโชว์ก้อน `firebaseConfig = { ... }` → **คัดลอกค่าทั้งหมด**
4. เมนูซ้าย **Build → Firestore Database → Create database**
   - เลือก location (เช่น `asia-southeast1`)
   - เลือก **Start in test mode** → Enable
   - *(test mode เปิดอ่าน/เขียน 30 วัน พอสำหรับ pilot — ค่อยใส่ rules จริงตอนใช้งานถาวร)*

## 2) วาง config ลงทั้ง 2 ไฟล์

เปิด `index.html` และ `counter.html` แก้ส่วน `firebaseConfig` ให้ตรงกับที่คัดลอกมา (ต้องเหมือนกันทั้ง 2 ไฟล์):

```js
const firebaseConfig = {
  apiKey: "AIza....",
  authDomain: "cinesnack.firebaseapp.com",
  projectId: "cinesnack",
  storageBucket: "cinesnack.appspot.com",
  messagingSenderId: "1234567890",
  appId: "1:1234:web:abc..."
};
```

## 3) ใส่เบอร์พร้อมเพย์ + ชื่อร้าน (เฉพาะ index.html)

```js
const SHOP = {
  name: "CineSnack",
  branch: "สาขาสยาม (ทดลอง)",
  promptpayId: "0812345678"   // เบอร์พร้อมเพย์ หรือเลขผู้เสียภาษี 13 หลัก
};
```
> QR สร้างจากเลขนี้ + ใส่ยอดอัตโนมัติ ลองสแกนเช็คกับแอปธนาคารก่อนเปิดจริง

## 4) (ถ้าจะใช้สินค้าจริง) แก้เมนูใน index.html

แก้ array `PRODUCTS` — แต่ละชิ้นมี: `name, desc, price, cat, best, img` และ `options` (ถ้ามีไซส์/รส)
รูป: ใส่ URL รูปจริง หรือปล่อย placeholder ไว้ก่อนก็ได้

---

## 5) เอาขึ้นเว็บ (Netlify Drop)

1. ไป https://app.netlify.com/drop
2. ลาก **โฟลเดอร์ `cinesnack-live` ทั้งโฟลเดอร์** วางลงไป (อย่าลากแค่ไฟล์เดียว)
3. ได้ลิงก์ เช่น `xxxx.netlify.app`
   - ลูกค้า: `xxxx.netlify.app/` (= index.html)
   - เคาน์เตอร์: `xxxx.netlify.app/counter.html`
4. ทำ QR ชี้ไปที่ลิงก์ลูกค้า ติดที่โต๊ะ/ป้าย

---

## วิธีใช้งานจริง

**เคาน์เตอร์ (iPad):** เปิด `/counter.html` ค้างไว้ → กด **🔔 เปิดเสียงเตือน** 1 ครั้ง (iOS ต้องกดก่อนเสียงถึงดัง)

**ลูกค้า:** สแกน QR → เลือกของ → จ่าย PromptPay → แนบสลิป → กดส่ง
→ ออเดอร์เด้งเข้า iPad + เสียงเตือนทันที

**ขั้นตอนที่เคาน์เตอร์:**
1. ออเดอร์ใหม่เข้าเลน "🆕 ออเดอร์ใหม่" → กด 🧾 ดูสลิป → เช็คยอดเข้าบัญชี
2. ถูกต้อง → กด **ยืนยัน → จัด** (ย้ายไปเลนกำลังจัด) / ผิด → กด ✕ ปฏิเสธ
3. จัดเสร็จ → กด **จัดเสร็จ → พร้อมรับ**
4. ลูกค้ามารับ → กด **ลูกค้ารับแล้ว ✓**
ลูกค้าเห็นสถานะอัปเดตสดทุกขั้นบนมือถือ

---

## เรื่องเลขคิว
- ทุกออเดอร์ได้ **เลขคิวรัน 1→100 แล้ววนกลับ 1** อัตโนมัติ (Firebase แจกให้แบบ atomic ไม่ซ้ำ)
- เคาน์เตอร์เรียกลูกค้าตามเลขนี้ (โชว์ตัวใหญ่บนการ์ด) — ลูกค้าก็เห็นเลขเดียวกันบนมือถือ
- Firebase สร้าง doc `counters/queue` ให้เองตอนมีออเดอร์แรก ไม่ต้องตั้งอะไรเพิ่ม
- อยากเปลี่ยนช่วงคิว (เช่น 1–200) แก้ `QUEUE_MAX` ใน `index.html`
- อยากรีเซ็ตคิวกลับเป็น 1 เอง: ใน Firestore console ลบ/แก้ค่า `n` ใน `counters/queue` เป็น 0

## หมายเหตุ (ก่อนใช้จริงจัง)
- **test mode หมดอายุ 30 วัน** — ถ้าใช้ต่อ ต้องตั้ง Firestore rules ที่ปลอดภัยขึ้น (จำกัดเขียนเฉพาะ field ที่ต้องการ ฯลฯ) บอกผมได้ตอนนั้น
- ระบบนี้ยัง **ไม่ตัดสต็อก/ไม่ลง POS** — เคาน์เตอร์คีย์เข้า POS เองตามเดิม (ตามที่ตั้งใจไว้)
- การยืนยันเงินเป็นแบบ **ดูสลิปด้วยตา** — ถ้าโตขึ้นค่อยต่อ gateway ให้ยืนยันยอดอัตโนมัติ
